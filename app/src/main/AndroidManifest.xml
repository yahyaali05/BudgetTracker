<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.BudgetTracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myapp">

        <!-- Start-Activity -->
        <activity
            android:name="com.example.BudgetTracker.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Weitere Activities -->
        <activity android:name="com.example.BudgetTracker.RegisterActivity" android:exported="true" />
        <activity android:name="com.example.BudgetTracker.DashboardActivity" android:exported="true" />
        <activity android:name="com.example.BudgetTracker.AddTransactionActivity" android:exported="true" />
        <activity android:name="com.example.BudgetTracker.ReportActivity" android:exported="true" />
        <activity android:name="com.example.BudgetTracker.StatisticsActivity" android:exported="true" />


    </application>
</manifest>
